<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Renderizado en Backend</title>

  <style>
    body {
      font-family: Arial, sans-serif; /* Cambia la fuente según tus preferencias */
      margin: 0;
      padding: 0;
      background-color: #222222; /* Cambia el color de fondo según tus preferencias */
    }

    header {
      background-color: #222222;
      color: white;
      text-align: center;
      padding: 1em;
    }

    h1 {
      margin-bottom: 1em;
    }

    ul {
      list-style: none;
      padding: 0;
      margin: 0;
      display: flex;
      flex-wrap: wrap;
      justify-content: space-around;
    }

    li {
      margin: 1em;
      display: flex;
      align-items: center;
      padding: 1em;
      border: 0.1px solid #fff;
      border-radius: 5px;
      background-color: #333;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      flex: 0 0 20%; /* Ajusta el ancho de cada elemento según tus preferencias */

    }

    img {
      max-width: 100%;
      height: auto;
      display: block;
      margin-bottom: 1em;
    }

    pre {
      background-color: #2a2a2a;
      color: aliceblue;
      padding: 1em;
      border-radius: 5px;
      overflow-x: auto;
    }

    code {
      margin-left: 0px;
      display: block;
      white-space: pre-wrap;
    }
  </style>
</head>
<body>
  <header>
    <h1>Rended by Backend</h1>
  </header>
  <ul>
    <% for (let i = 0; i < objects.length; i++) { %>
      <li>
        <img src="<%= objects[i].emoji_url %>" alt="<%= objects[i].emoji_name %>">
<pre style="margin-left: 20px; width: 230px; height: 90px;">
<code>{
"id": <%= objects[i].id %>,
"emoji_name": <%= objects[i].emoji_title %>,
"emoji_url": <a href="<%= objects[i].emoji_url %>">link</a>
}</code>
</pre>
      </li>
    <% } %>
  </ul>
</body>
</html>
